{
  "name": "lightTribeDocs",
  "version": "1.0.0",
  "description": "Swagger-UI static pages for LightTribe REST interface",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "npm install swagger-ui && cp -r ./node_modules/swagger-ui/dist/* . && git checkout develop -- api/swagger.json && git add -A && git commit -m 'Updated docs using npm script publish'",
    "publish": "git push github gh-pages"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "swagger-ui": "^2.1.2-M2"
  }
}
